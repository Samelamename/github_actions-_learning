
name: Bash workflow

on: [push]
jobs:
  actions_demo:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        ref: my-branch
    - run: pwd
    - run: chmod +x bash.sh
    - run: ./bash.sh
