
name: Bash workflow

on: [push]
jobs:
  actions_demo:
    runs-on: ubuntu-latest
    steps:
    - run: pwd
    - run: actions/checkout@v3
    - run: chmod +x bash.sh
    - run: ./bash.sh
